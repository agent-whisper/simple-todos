{
  "name": "@simple-todos/api",
  "version": "0.0.0",
  "private": true,
  "//better-sqlite3": "Pinned below the current major: 13.x segfaults node.exe on the Windows dev host; verified NOT a Linux problem. See docs/better-sqlite3-pin.md before changing.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": {
    "@fastify/rate-limit": "^11.2.0",
    "@fastify/static": "^10.1.3",
    "@node-rs/argon2": "^2.2.0",
    "@simple-todos/shared": "*",
    "better-sqlite3": "^11.10.0",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.12.1",
    "jose": "^6.2.10",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.23.13"
  }
}
