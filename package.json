{
  "name": "simple-todos",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w @simple-todos/shared",
    "pretest": "npm run build:shared",
    "test": "vitest run",
    "typecheck": "npm run build:shared"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
